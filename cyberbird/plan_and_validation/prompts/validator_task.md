Alert from {scanner}, rule {rule}.

  file:     {file}
  line:     {line}
  message:  {message}

The candidate patch:

{patch}

The runtime's mechanical checks:

{checks}

What the agent did to produce it:

{trajectory}

Decide whether to release this patch.
