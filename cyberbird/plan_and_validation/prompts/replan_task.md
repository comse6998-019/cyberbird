Alert from {scanner}, rule {rule}.

  file:     {file}
  line:     {line}
  message:  {message}

Your previous plan was:

{plan}

{reason}

The observation that kept coming back:

{last_result}

Revise the plan. Keep what has already been established, and drop or replace the
steps that led here. The same approach will return the same observation, so the
revised plan has to reach the goal another way.
